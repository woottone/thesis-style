<html>
  <head>
      <link rel="stylesheet" type="text/css" href="public/css/style.css" />
      <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.2.0/socket.io.js"></script>
      <script>
        const socket = io();

        socket.on('test', data => {
          console.log('socket test', data);
        });

        socket.on('reload', () => {
          window.location.reload();
        });
      </script>
  </head>
  <body>
    <!-- I coded all of the HTML/CSS, anywhere that has javascript was done by the developer -->
  <div class="new-player-container mobile-container">
    <!-- <img src="static/logo-bubble.png" class="mobile-header-bubble"> -->
    <div class="logo-background"></div>
    <label for="name" class="name-header body-text">Enter your name</label>
    <input id="name" class="name-input">
    <button id="submit" class="name-submit button" ><p style="padding-bottom: 50%;">submit</p></button>
  </div>
  </body>
  <script>
    const nameInput = document.getElementById('name');
    document.getElementById('submit').addEventListener('click', () => {
      const name = nameInput.value;
      document.cookie = `name=${name}`;
      socket.emit('new-player', name);
      // emitting this event (new-player) and the data being sent is the name from the cookie
    });
  </script>
</html>
